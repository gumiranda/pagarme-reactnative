import styled from 'styled-components/native';
import {appColors} from '../../utils/appColors';

export const Container = styled.View`
  padding: 0 15px;
  height: 54px;
  background: ${appColors.white90};
  border-radius: 4px;
  flex-direction: row;
  align-items: center;
`;
